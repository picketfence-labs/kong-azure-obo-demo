# docker compose用の環境変数。コピーして .env を作成する（.envはコミット対象外）。
#   cp .env.example .env

# Kong Gateway自身のPostgres接続パスワード（ローカル検証用の任意の値でよい）
KONG_PG_PASSWORD=

# Kong Enterpriseライセンス（JSON文字列、または `deck` 同様1行で貼り付け）
KONG_LICENSE_DATA=
